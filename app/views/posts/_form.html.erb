<%= form_for [@customer, @post] do |f| %>
	<div class="form-group">
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div class="form-group">
		<%= f.label :body %>
		<%= f.text_area :body %>
	</div>
	<div>
		<%= f.submit %>
	</div>
<% end %>